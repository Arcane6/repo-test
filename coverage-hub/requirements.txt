Flask==3.1.1
oracledb==3.2.0
google-cloud-bigquery==3.42.2
pandas==2.3.1
python-dotenv==1.1.1
gunicorn==23.0.0

# Agente de IA (aba "Assistente" do Acesso Móvel) — modules/mobile_access
# usa google-adk (Agent Development Kit) contra Vertex AI (GOOGLE_GENAI_USE_VERTEXAI
# no .env), executando SQL somente-leitura sobre um DataFrame via duckdb
# (dados vindos direto do Oracle — MUNICIPIOS_FECHAMENTO e
# REL_CIDADES_PLANEJADO_26, ver agent/excel_municipios.py) e formatando
# resultado em markdown (tabulate). requests + lxml alimentam as
# ferramentas de Teleco/notícias (scraping de HTML/RSS público).
google-adk==2.5.0
duckdb==1.5.5
requests==2.34.2
lxml==6.1.1
tabulate==0.10.0
